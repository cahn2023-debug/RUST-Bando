{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Read-only dialog access for the standalone Basemap Preview",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
