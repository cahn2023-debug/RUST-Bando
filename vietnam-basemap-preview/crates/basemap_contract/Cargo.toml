[package]
name = "basemap_contract"
version = "0.1.0"
edition = "2021"
description = "Shared manifest and release contract for the Vietnam Basemap Platform"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
