# Sprite Inputs

Sprite metadata is kept in basemap.json; the corresponding PNG is a release
input generated from the SVG sources in assets/icons/. The pair is served
from the same package-relative basename.

The source icons are intentionally provider-independent. The operator must
preserve the applicable license and attribution when generating the PNG.
