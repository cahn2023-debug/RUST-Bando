[package]
name = "basemap_release"
version = "0.1.0"
edition = "2021"
description = "Validated activation and rollback for Vietnam Basemap releases"
license = "MIT"

[dependencies]
basemap_contract = { path = "../basemap_contract" }
basemap_offline = { path = "../basemap_offline" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
