# Shared packages

This directory is an explicit empty baseline until a module is proven to be used by at least two application tabs or tools. App-specific code stays with its owning tab; speculative extraction is not allowed.

