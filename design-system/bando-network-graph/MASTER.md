# DEPRECATED — không dùng file này

File này được sinh tự động bởi công cụ scaffold design-system cho `bando-graph-viewer` (một VS Code extension riêng, không phải app chính). Palette trong bản gốc (`#3B82F6` primary, `#F97316` CTA, nền sáng, font Fira Code/Fira Sans, style "Glassmorphism") chưa từng được triển khai.

Nguồn sự thật duy nhất cho design là:

> **`design-system/MASTER.md`**

Bản gốc được giữ tại `BAK/design-system-legacy/bando-network-graph-MASTER.md` để tham chiếu lịch sử.

Nếu bạn là agent/LLM đang đọc file này: hãy đọc `design-system/MASTER.md` và bỏ qua toàn bộ nội dung đã bị xoá ở đây.
