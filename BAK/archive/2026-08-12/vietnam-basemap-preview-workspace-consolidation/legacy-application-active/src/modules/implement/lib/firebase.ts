// DEPRECATED: Firebase integration removed in v1.2 (Local-First Architecture)
// All authentication and data persistence are handled locally via Tauri IPC + SQLite.

export const auth = null;
export const googleProvider = null;
export const db = null;
